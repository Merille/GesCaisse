@model List<EasytransitCaisse.Models.Utilisateur>
@{
    ViewData["Title"] = "Utilisateurs";
}
<div class="container mt-4">

    <div class="d-flex justify-content-between mb-3">
        <h3>Liste des Utilisateurs</h3>

        <a href="/Utilisateur/Create" class="btn btn-primary">
            + Nouvel utilisateur
        </a>
    </div>

    <div class="card shadow-sm">

        <table id="UsersTable" class="table table-hover align-middle mb-0">
            @* <table class="table table-hover align-middle mb-0"> *@
            <thead class="table-dark">
                <tr>
                    <th>Nom utilisateur</th>
                    <th>Nom complet</th>
                    <th>Profil</th>
                    <th>Droits</th>
                    <th width="180">Actions</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var u in Model)
                {
                    <tr>
                        <td>@u.NomUtilisateur</td>
                        <td>@u.NomComplet</td>
                        <td>
                            <span class="badge bg-info">@u.Profil</span>
                        </td>
                        <td>
                            @if (u.PeutValiderOperations)
                            {
                                <span class="badge bg-success">Validation caisse</span>
                            }
                        </td>
                        <td>
                            <div class="d-flex gap-1">
                                <a href="/Utilisateur/Edit/@u.Id" class="btn btn-warning btn-sm">
                                    Modifier
                                </a>

                                <form asp-action="Delete" asp-route-id="@u.Id" method="post" class="d-inline"
                                      onsubmit="return confirm('Supprimer ?')">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-danger btn-sm">
                                        Supprimer
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>

        </table>

    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#UsersTable').DataTable({
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.6/i18n/fr-FR.json'
                },
                pageLength: 10,
                lengthMenu: [10, 25, 50, 100],
                order: [[1, 'asc']], // Tri par défaut sur Société
                columnDefs: [
                    { orderable: false, targets: 4 } // Colonne Actions non triable
                ],
                dom: '<"d-flex justify-content-between align-items-center mb-3"lf>rtip'
            });
        });
    </script>
}